# inventory
# Defines target hosts and variables for Ansible.
[vps]
# VPS IP and SSH user (SSH key provided via agent forwarding)
178.16.142.165 ansible_user=dabernathy

[vps:vars]
# User on the VPS that will run remote docker commands (must exist and have sudo/docker group access)
deploy_user=dabernathy
# Configure sudo password prompt (will ask for password interactively)
ansible_become_method=sudo
ansible_become_ask_pass=true
