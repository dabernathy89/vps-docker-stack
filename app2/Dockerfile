# Dockerfile (for App 2)
# Builds the container image for the second FrankenPHP application.

# Use the official FrankenPHP image as a base
# Choose specific PHP version if needed
FROM dunglas/frankenphp:latest-php8.3

# Copy your application's public files into the container's document root.
COPY public/ /app/public/

# Optional: Copy other application files if needed
# COPY . /app
